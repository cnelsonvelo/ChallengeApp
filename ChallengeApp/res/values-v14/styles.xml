<resources>

    <!--
        Base application theme for API 14+. This theme completely replaces
        AppBaseTheme from BOTH res/values/styles.xml and
        res/values-v11/styles.xml on API 14+ devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Holo">
        <!-- API 14 theme customizations can go here. -->
        <item name="android:actionBarStyle">@style/AppActionBar</item>
    </style>
    
    <!--  Action Bar Style -->
    <style name="AppActionBar" parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/action_bar_background</item>
        <item name="android:titleTextStyle">@style/AppActionBarText</item>
    </style>

    <!--  Action Bar Text Style -->
    <style name="AppActionBarText" parent="@android:style/Widget.Holo.ActionBar.Solid">
        <item name="android:textColor">@color/action_bar_text</item>
    </style>

</resources>
